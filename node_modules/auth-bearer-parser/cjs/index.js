"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const http_error_js_1 = __importDefault(require("./lib/http-error.js"));
const authBearerParser = (option) => (req, res, next) => {
    const { authorization } = req.headers;
    if (!authorization) {
        if (option?.isThrowError)
            throw new http_error_js_1.default(401, `authorization header missing`);
        return next();
    }
    const [type, token] = authorization.split(/\s/, 2);
    if (type !== 'Bearer') {
        if (option?.isThrowError)
            throw new http_error_js_1.default(400, `invalid token type: ${type}`);
        return next();
    }
    if (!token) {
        if (option?.isThrowError)
            throw new http_error_js_1.default(401, `token missing`);
        return next();
    }
    req.token = token;
    return next();
};
exports.default = authBearerParser;
//# sourceMappingURL=index.js.map