import { Request, Response, NextFunction } from 'express';
declare module 'express-serve-static-core' {
    interface Request {
        token: string | undefined;
    }
}
export interface BearerParserOptions {
    /**
     * @default false
     * @description If true, throw error when bearer token is invalid.
     */
    isThrowError?: boolean;
}
declare const authBearerParser: (option?: BearerParserOptions) => (req: Request, res: Response, next: NextFunction) => void;
export default authBearerParser;
